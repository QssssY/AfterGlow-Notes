# 动态数据服务（server/ 的 Go 后端）地址，构建时烧进产物（PUBLIC_ 前缀 = 进浏览器）。
# 取值：
#   http://127.0.0.1:8787   本地开发：astro dev 跑 4321，服务跑 8787，跨域直连
#   https://api.你的域名     生产（分体，推荐）：前端 dist 丢 Vercel / CF Pages / GH Pages，
#                           API 在小机子 —— 在托管平台的构建环境变量里设这个
#   same-origin             生产（备选，同源）：Go 服务 -site dist 整站自托管时用；
#                           也是本地预演生产形态的用法（见 server/README.md）。
#                           Linux 下写 / 等价；Windows Git Bash 只能用 same-origin
#                           （单个 / 会被 MSYS 路径转换偷换成 Git 安装目录）
#   （不设）                 纯静态模式：点赞只存访客浏览器，阅读数与「大家在看」不显示数字；
#                           管理台 /overview 自动切换成 GitHub 直连（细粒度令牌登录，
#                           保存即提交）—— 本地想连 Go 管理台就必须设本地址，不再默默回落
PUBLIC_API_BASE=http://127.0.0.1:8787

# 音乐来源前缀（可选）。音乐是版权物不进仓库（public/music 已 gitignore）：
#   不设                     本地开发 / 单机全站：public/music 在磁盘上，同源可达
#   https://api.你的域名      分体部署：页面在平台 CDN（产物里没有音乐），
#                            歌从小机子的 -music 目录走（见 server/README.md）
# PUBLIC_MUSIC_BASE=
